const firstYearCompleted = true;
const yearsCompleted = 1;

console.log(yearsCompleted == firstYearCompleted) // mette a paragone il tipo di variabile, per capire se 1 corrisponde a true infatti risulterà vero
console.log(yearsCompleted === firstYearCompleted) // qui invece con il terzo uguale, chiede se i due valori sono perfettamente identici in tutto e per tutto, anche il tipo. Ma essendo un booleano e un numero risulterà false

