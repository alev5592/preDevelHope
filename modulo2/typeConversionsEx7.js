let hello = 'Ciao';     // number
let age = 18;       // boolean
let isGraduated = false;     // string

console.log(Number(hello)); // la funzione ci sta dicendo che il valore della variabile non può essere convertita in un numero

console.log(Boolean(age));

console.log(String(isGraduated));